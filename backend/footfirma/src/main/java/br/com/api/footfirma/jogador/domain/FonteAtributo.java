package br.com.api.footfirma.jogador.domain;

/**
 * {@code GERADO} não é sinônimo de {@code ESTIMADO}: estimado é inferido a partir de
 * alguma observação, gerado não observa nada — nasce de uma semente.
 */
public enum FonteAtributo {
    IMPORTADO, ESTIMADO, GERADO
}
